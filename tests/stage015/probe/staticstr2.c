/* 静的な多次元 char 配列を文字列で初期化する形。**まだ直っていない。**
 *
 * ginit() は多次元を平らに畳んでから要素を読む。畳んだ後の要素型は
 * char であって配列ではないので，cc15q が ginit1() に入れた
 * 「対象が配列なら実体を並べる」の判断が効かない。結果，要素 1 つに
 * 4 バイトの再配置が書かれて中身がアドレスになる。
 *
 * **通ってしまうが結果が誤っている** —— 台帳で bad と呼ぶ，いちばん
 * 悪い状態である。tcc はこの形を使わないので世代を刻んでいない。
 * ここは **今どう壊れているかを測って台帳に載せる** ためのもので，
 * 直したら台帳の側を bad から ok へ書き換える。
 *
 * 直った形は staticstr.c にある。 */
int putc(int c);

int g(void) {
  static char m[2][8] = { "ab", "cd" };
  return (m[0][0] == 'a' && m[1][0] == 'c') ? 'y' : 'n';
}

int main() { putc(g()); putc('\n'); return 0; }
