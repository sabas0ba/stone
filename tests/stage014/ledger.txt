# 適合台帳 (docs/stage014-external.md 4 章)
#
# 現実の C が使う書き方に対して，本処理系が今どう振る舞うかを記録する。
# 1 行 1 検査で，欄は「名前 状態 期待 説明」。
#
#   ok    <出力>   コンパイル・実行が通り，標準出力がその文字列になる
#   gap   <cc の終了コード>  cc がその終了コードで拒む (未対応。拒むこと自体は正しい)
#   bad   <出力>   通ってしまうが結果が誤っている (**いちばん悪い状態**)
#
# 状態が変わったらこの表を直す。表と実測が食い違えば検査は失敗する。
# 「直したのに表がそのまま」も「壊したのに気づかない」も等しく捕まえる。

# ---- 通るもの (現実の C でよく使われ，本処理系が既に扱えるもの) ----
escapes      ok   AB\t\\\n
gotoout      ok   4\n
ppcond       ok   7ok\n
ppelif       ok   c\n
ptrdiff      ok   abc\n
sizeofx      ok   784\n
strtab       ok   hiyo\n
switchfall   ok   139\n
unionuse     ok   10\n
vaforward    ok   6\n
unsignedops  ok   abcd\n
multidim     ok   6\n
cvquals      ok   3\n
multidecl    ok   7\n
strcat_lit   ok   abcd\n
usuffix      ok   1y\n
registerkw   ok   3\n

# ---- 未対応 (cc が拒む) ----
nestinit     gap  1    入れ子の初期化子 ({{1,2},{3,4}})
fptab        gap  1    関数名を初期化子に使う (関数ポインタの表)
staticloc    gap  1    関数内 static
kr_func      gap  1    K&R 形式の関数定義
bitfield     gap  1    ビットフィールド
selfref      gap  2    不完全型への typedef の前方参照
floatv       gap  2    浮動小数点 (float / double)
emptyproto   gap  5    引数リストが空の宣言へ実引数を渡す
llwidth      gap  2    long long (64 bit の型は無い。通さず拒む)

# ---- 通るが誤り ----
# いまは無い。llwidth は第 2 部で「通るが 32 bit」から「拒む」へ移した
# (docs/stage014-external.md 5.2)
