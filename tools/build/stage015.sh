# stage015 のビルド手順。実体は 3 つに分けてある。
#
#   stage015a  第 1〜4 部で封じた cc の世代 (cc15a〜cc15k)。原文が
#              変わらないので普段は必ず cached になる。いちばん重い
#   stage015b  第 6 部の最前線 (cc15l〜cc15n・pp15・pp16・ld15)
#   stage015c  OS の側 (実行時支援・カーネル・libc15)。手を入れる回数が
#              いちばん多い
#
# 分ける理由: スタンプの入力には手順のファイル自身が入る。1 枚だった頃は
# カーネルの 1 行を直しても cc の 14 世代が作り直しになり，30 分以上
# かかっていた (docs/dev-notes.md 1.4)。
#
# 依存は a -> b -> c の 1 本道なので順に呼ぶ。

do_stage015() {
    do_stage015a
    do_stage015b
    do_stage015c
}
