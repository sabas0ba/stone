# Stage 11: フリースタンディング libc 設計文書

## 1. 目的と位置づけ

### 1.1 目的

Stage 10 で C89 の**言語**が揃った。しかし C のプログラムは言語だけでは
書けない。`memcpy` も `strlen` も `malloc` も無い状態では，これまでの
コンパイラ自身がそうだったように，すべてを手書きの走査で済ませることに
なる。

Stage 11 の目的は **標準ライブラリの中核を自作の C で用意する** ことである。
[roadmap.md](roadmap.md) Phase B の 1 段目であり，これが済めば「他人の書いた
C」が要求するものの半分が揃う。

### 1.2 フリースタンディングとは

C89 は処理系を 2 種類に分ける。

| | 要求されるヘッダ |
|---|---|
| ホスト形式 (hosted) | 標準ヘッダすべて。`main` が呼ばれ `stdio` が使える |
| **フリースタンディング** | `float.h` `limits.h` `stdarg.h` `stddef.h` のみ |

現時点の実行環境はベアメタルで，ファイルも標準入出力もない。したがって
まずフリースタンディングを名乗り，そのうえで **OS を要さない範囲の関数**
(`string.h` / `ctype.h` / `stdlib.h` の一部) を足す。`stdio.h` は
syscall を獲得する Stage 12 の課題である。

### 1.3 分割

| 部 | 内容 |
|---|---|
| 第 1 部 | 土台と純粋な関数: `stddef.h` `limits.h` `string.h` `ctype.h` (3 章) |
| 第 2 部 | 記憶域: `malloc` / `free` / `calloc` / `realloc` |
| 第 3 部 | 整列と探索・数値変換: `qsort` `bsearch` `atoi` `strtol` `abs` `div` |

第 1 部を先に置くのは，**どれも状態を持たない** からである。ヒープの設計
(第 2 部) や比較関数を介した呼出し (第 3 部) と違い，入力から出力が一意に
決まるので，検証が値の照合だけで済む。libc の置き場・ビルドの通し方・
テストの型をここで決めてしまい，後続はその上に足す。

### 1.4 非目標

- `stdio.h` (ファイル・標準入出力)。syscall が要るので Stage 12
- 浮動小数点を扱うもの (`math.h`, `strtod`, `%f`)。RV32I**M** に FPU が無く，
  ソフトウェア浮動小数点そのものが独立した課題である
- ロケール (`setlocale`)・シグナル (`signal.h`)・`setjmp`
- ワイド文字 (`wchar.h`)

## 2. 構成

### 2.1 置き場

```
libc/include/   ヘッダ (利用者から見える面)
  stddef.h      size_t, NULL, offsetof
  limits.h      CHAR_BIT, INT_MAX, ...
  stdarg.h      (Stage 10 第 3 部の 2 で追加済み)
  string.h
  ctype.h
libc/src/       実装 (自作の C)
  string.c
  ctype.c
```

ヘッダと実装を分けるのは，**利用者が見るものと処理系が持つものを分ける**
ためである。`libc/include/` は前処理で取り込まれ，`libc/src/` は
コンパイルされてオブジェクトになる。

置き場は世代ごとに凍結され，`stage011/libc/` `stage012/libc/` … と
Stage 名の下に置かれる ([artifacts.md](artifacts.md))。本章以降で
`libc/include/x.h` と書いたものは，第 11 世代では
`stage011/libc/include/x.h` を指す。

### 2.2 リンクの通し方

ld は既に複数のオブジェクトを受け取れる (Stage 8)。したがって libc は
**オブジェクトを並べるだけ** で使える。アーカイブ (`.a`) の形式と `ar` は
Stage 13 の課題で，ここでは要らない。

```
pp(stddef.h string.h string.c) | cc > string.o
pp(... prog.c)                 | cc > prog.o
ld prog.o string.o             > prog.bin
```

必要なオブジェクトだけを並べる形なので，使わない関数は最初からリンクされ
ない。アーカイブの「必要なメンバだけ取り出す」働きを，人が並べることで
代用している。

### 2.3 純度ポリシー

これまでどおり，ホストの `as` / `ld` / libc は使わない。`lib/` は自作の
C で書き，自作の cc でコンパイルし，自作の ld でリンクする。

## 3. 第 1 部の設計: 土台と純粋な関数

### 3.1 `stddef.h`

```c
typedef unsigned size_t;
typedef int ptrdiff_t;
#define NULL 0
#define offsetof(t, m) ((size_t)&(((t *)0)->m))
```

`size_t` を `unsigned` にするのは，この処理系のポインタが 32 ビットで
`unsigned` も 32 ビットだからである。`offsetof` は「0 番地に置いた構造体の
メンバのアドレス」という慣用的な方法で書ける。これが動くのは Stage 10 第 3 部の
3 でメンバ参照がアドレス計算に落ちるようになったためである。

なお，この定義はメンバ m が配列のとき `&` を配列に適用する。着手時の
処理系はこれを一律にエラーとしていたため，Stage 10 に補遺 2
([stage010-c89.md](stage010-c89.md) 20 章 / cc10l) を追加してから進めた。

`NULL` を `((void *)0)` ではなく `0` にしているのは，`void *` との比較や
代入で余計な型検査に引っかからないようにするためである。

### 3.2 `string.h` の実装方針

| 関数 | 方針 |
|---|---|
| `memcpy` | 前から 1 バイトずつ。領域が重なる場合の動作は未定義 (C89 どおり) |
| `memmove` | 重なりを見て前から / 後ろからを選ぶ |
| `memset` | 前から 1 バイトずつ |
| `memcmp` | `unsigned char` として比較する |
| `strlen` | NUL まで走る |
| `strcpy` / `strncpy` | `strncpy` は n に満たない分を NUL で埋める (C89 どおり) |
| `strcmp` / `strncmp` | `unsigned char` として比較する |
| `strchr` / `strrchr` | 終端の NUL も探索対象に含む (C89 どおり) |
| `strcat` / `strncat` | 末尾を探してから複写する |
| `strstr` | 素朴な二重走査 |
| `strspn` / `strcspn` / `strpbrk` | 文字集合を 256 バイトの表に展開する |

**比較を `unsigned char` で行う** のは C89 の要求である。この処理系の
素の `char` は符号なしなので実害は出ないが，仕様どおりに書いておく。
`char` の符号は処理系定義で，移す先が変われば結果が変わる。

速度のための語単位の複写は入れない。整列していない領域の扱いが増え，
得られるものに対して複雑すぎる。必要になった時点で `memcpy` だけ差し替える。

### 3.3 `ctype.h` の実装方針

C89 の `is*` は **`int` を取り，`unsigned char` の値か `EOF` でなければ
未定義** である。表を引く実装が普通だが，ここでは範囲比較で書く。

- 表 (256 バイト) を持つと，使わなくても `.bss` か `.text` を消費する
- 範囲比較なら分岐数個で済み，どの関数も独立にリンクできる

`EOF` (-1) を渡しても誤動作しないよう，範囲比較では必ず下限も検査する。

ロケールは持たないので，`isalpha` は ASCII の英字だけを真とする。

### 3.4 なぜ `restrict` も `const` も付けないか

C89 に `restrict` は無い (C99)。`const` は Stage 10 第 2 部の 1 で
**構文としては受けるが意味論は持たない**。宣言に付けても検査されないので，
付けると「検査されている」という誤解を生む。付けない。

## 4. 成果物

- `libc/include/stddef.h` `libc/include/limits.h` `libc/include/string.h` `libc/include/ctype.h`
- `libc/src/string.c` `libc/src/ctype.c`
- `tmp/build/string.o` `tmp/build/ctype.o` (ビルド生成物)
- 第 2 部: `libc/include/stdlib.h` `libc/src/stdlib.c` `tmp/build/stdlib.o` (7 章)。
  `morecore` は第 11 世代では `stdlib.c` の中の内部関数であり，独立した
  `morecore.c` になるのは Stage 12 で `brk` を獲得してからである
  (`stage012/libc/src/morecore.c`)
- 第 3 部: 同上へ追加 (8 章)

## 5. 検証計画 (tests/stage011)

テストの素材は Stage 10 と同じく用途で分ける (`src/` と `expected/`)。

1. **ビルド再現**: `string.o` / `ctype.o` の SHA-256 が各 `.md` 記載値と一致
2. **値の照合**: 各関数を境界込みで呼び，結果を出力して照合する。
   とくに次を落とさない。
   - `memmove` の重なり (前へ・後ろへ・完全一致)
   - `strncpy` の詰め物 (n に満たないとき NUL で埋まること)
   - `strchr(s, 0)` が終端を指すこと
   - `memcmp` / `strcmp` が `unsigned char` として比較すること
     (0x80 以上を含む文字列で符号付き比較と結果が変わる)
   - `is*` に `EOF` (-1) を渡しても偽を返すこと
3. **リンクの単位**: 使わない翻訳単位を並べなくてもリンクが通ること
   (`string.o` だけで済むプログラムに `ctype.o` を渡さない)
4. **自己適用**: libc を使って書いた小さなプログラムが動くこと

`0` 除算のような未定義動作の検査は行わない。

## 6. Stage 12 への接続

第 3 部まで終えると，OS を要さない範囲の標準ライブラリが揃う。
残るのは `stdio.h` と，その下の syscall である。Stage 12 で
[roadmap.md](roadmap.md) 2.2 の案 X (Linux ユーザランド) を決め，
`open` / `read` / `write` / `close` / `brk` を獲得する。

そのとき **`malloc` の下回りが差し替わる** (第 2 部で固定の領域を使って
おき，Stage 12 で `brk` へ繋ぎ替える)。この境界を第 2 部の設計で意識しておく。

## 7. 第 2 部の設計: 記憶域 (malloc / free / calloc / realloc)

### 7.1 ヒープの供給源と Stage 12 への境界

現時点の実行環境はベアメタルで，記憶域を要求する相手 (OS) がいない。
そこで **ヒープは `.bss` 上の固定領域** とする。`lib/stdlib.c` に
1 MiB の大域配列を置き，そこから配る。`.bss` は実行時に 0 で埋まっている
前提 ([stage008-elf-ld.md](stage008-elf-ld.md) 3 章) なので，割付け器の
内部状態 (フリーリストの先頭) も初期値 0 のまま使い始められる。

領域の供給は内部関数 `morecore` だけが行う。Stage 12 で `brk` を獲得した
とき差し替えるのはこの 1 関数であり，`malloc` / `free` 本体は変わらない
(6 章)。

### 7.2 割付け器: フリーリストの first-fit

K&R (第 2 版 8.7 節) の割付け器を土台にする。単純で，`free` による併合
まで含めて枯れた設計だからである。

- 各ブロックは 8 バイトのヘッダ (次のフリーブロックへのポインタ，
  ヘッダ込みの大きさ) を持つ。大きさの単位はヘッダの大きさ (8 バイト)
- フリーリストはアドレス順の循環リスト。`malloc` は first-fit で探し，
  大きいブロックは末尾を切り出して返す
- `free` はアドレス順の挿入位置を探し，前後のブロックと隣接していれば
  併合する。これでヒープ全体を解放すれば 1 ブロックに戻る
- 領域が尽きたら `morecore` が固定領域から補充する (最小 512 単位)。
  補充できなければ `malloc` は `NULL` を返す

整列は 4 バイト (この処理系の最大整列。`double` が無いため) である。
`.bss` の割付けはオブジェクトごとに 4 バイト境界へ丸められ，単位が
8 バイトなので，返すアドレスは常に 4 の倍数になる。

### 7.3 各関数の仕様 (C89 7.10.3 との対応)

| 関数 | 仕様 |
|---|---|
| `malloc(n)` | `n == 0` は `NULL` を返す (処理系定義。本実装は「確保しない」を選ぶ) |
| `free(p)` | `p == NULL` は何もしない (C89 どおり)。それ以外は `malloc` 系が返した値のみ有効 |
| `calloc(nmemb, size)` | 積が `size_t` に収まらなければ `NULL`。確保後に明示的に 0 で埋める (再利用ブロックは 0 とは限らない) |
| `realloc(p, n)` | `p == NULL` は `malloc(n)`。`n == 0` は `free(p)` して `NULL`。現ブロックに収まるなら在所のまま返し，収まらなければ確保・複写・解放する |

`realloc` の複写は内部ループで行い，`memcpy` を呼ばない。呼ぶと
`stdlib.o` を使うすべてのプログラムが `string.o` のリンクを強制される
(未解決シンボルはリンクエラーになる) ためである。同じ理由で `calloc` の
0 埋めも `memset` を呼ばない。

### 7.4 `stdlib.h` は部分実装

第 2 部で宣言するのは上記 4 関数だけである。整列と探索・数値変換
(`qsort` / `bsearch` / `atoi` / `strtol` / `abs` / `div`) は第 3 部で，
`exit` / `abort` / `atexit` は実行環境に依存するため Stage 12 で扱う。

### 7.5 検証計画 (tests/stage011 への追加)

1. **ビルド再現**: `stdlib.o` の SHA-256 が `lib/stdlib.md` 記載値と一致
2. **値の照合** (`src/mal.c`)。アドレスの具体値には依存せず，仕様として
   観測できる性質を検査する。
   - 返るアドレスが 4 の倍数であること
   - 同時に生きているブロックへの書込みが互いを壊さないこと
   - `free` 直後に同じ大きさを `malloc` すると同じアドレスが返ること
     (first-fit の確認)
   - `malloc(0)` / `calloc(0, n)` / 積が溢れる `calloc` が `NULL` を返すこと
   - `calloc` の領域が 0 で埋まっていること (一度使って解放した領域の
     再利用で確認する)
   - `realloc` が内容を保持すること。縮小が在所のまま返ること。
     `realloc(NULL, n)` と `realloc(p, 0)` の縮退
   - `free(NULL)` が何もしないこと
   - 総量を超える要求が `NULL` を返すこと
   - **枯渇と回復**: 尽きるまで確保して `NULL` を確認し，全て解放した後に
     ヒープほぼ全量の 1 ブロックが確保できること (併合がヒープ全体で
     機能している証明)
3. **リンクの単位**: `mal` は `stdlib.o` だけでリンクが通ること

`free` への不正なポインタ，解放済み領域の使用など，未定義動作の検査は
行わない。

## 8. 第 3 部の設計: 整列と探索・数値変換

第 3 部の関数 (`qsort` / `bsearch` / `atoi` / `strtol` / `abs` / `div`) は
すべて `stdlib.c` へ追加する。記憶域を確保しないため第 2 部の割付け器
とは独立であり，`stdlib.o` は引き続き単独でリンクできる。

### 8.1 `qsort` は Shell ソートで実装する

`qsort` の名前は API のものであり，C89 はアルゴリズムを規定しない
(安定性も要求されない)。ここでは Shell ソート (ギャップ列 n/2, n/4, ...)
を選ぶ。

- **非再帰**: クイックソートの再帰は最悪で要素数に比例する深さの
  フレームスタックを使う。Shell ソートはループのみで，スタック消費が
  要素数に依存しない
- **追加記憶域なし**: マージソートのような作業領域が要らない
- 最悪 O(n^2) だが，想定する用途 (Stage 13 の道具が扱う数千要素程度の
  表) には足りる。必要になった時点でギャップ列の改良か heap ソートへの
  差し替えで済み，API は変わらない

要素の交換はバイト単位で行い，要素の大きさと整列に制約を付けない。
比較は関数ポインタで受ける (Stage 10 第 2 部の 5)。

### 8.2 `bsearch`

通常の二分探索である。C89 どおり，比較関数の第 1 引数が探索キー，
第 2 引数が配列要素を指す。一致する要素が複数ある場合にどれを返すかは
規定されない (C89 どおり)。

### 8.3 `strtol` / `atoi`

C89 7.10.1.5 の構文 (空白の読み飛ばし，符号，基数 2〜36，基数 0 の
自動判定，`0x` 前置，`endptr`) を実装する。

- 空白の判定は内部で行い，`ctype.o` に依存しない (7.3 と同じ理由)
- **溢れは `LONG_MAX` / `LONG_MIN` への飽和のみ** で表す。C89 は
  `errno = ERANGE` も要求するが，`errno` は実行環境 (Stage 12) を
  待って導入する。それまで飽和値との等値比較だけでは溢れと真の
  `LONG_MAX` を区別できないことを許容する
- 数字が 1 桁も無ければ 0 を返し，`*endptr` には先頭 (`s` そのもの) を
  書く (C89 どおり)
- 蓄積は `unsigned` で行い，桁を足す前に上限を検査する。負のときの
  上限 2147483648 は `long` に収まらないためである

`atoi(s)` は `(int)strtol(s, NULL, 10)` と等価に実装する (C89 どおり)。
`long == int` (docs/stage010-c89.md 12 章) のため `atol` / `labs` /
`ldiv` は別名にすぎず，必要になった時点で足す (現時点では非目標)。
`strtoul` / `rand` / `srand` も第 3 部の範囲外である。

### 8.4 `abs` / `div`

`abs(INT_MIN)` は未定義 (C89 どおり)。`div` は構造体 `div_t`
(`quot` / `rem`) を値で返す。これが書けるのは Stage 10 第 3 部の 4
(構造体の返却) による。RV32M の `div` / `rem` は 0 方向への切捨てで，
C89 が `div` に要求する `quot * denom + rem == numer` と一致する。

### 8.5 検証計画 (tests/stage011 への追加)

1. **ビルド再現**: 従来どおり `stdlib.o` の SHA-256 照合 (値は更新される)
2. **整列と探索** (`src/srt.c`): 逆順・整列済み・重複あり・全要素同値の
   int 配列，要素の大きさが 4 の倍数でない構造体配列，nmemb が 0 / 1 の
   縮退。`bsearch` は両端・中央・不在・nmemb 0
3. **数値変換** (`src/num.c`): 空白と符号，基数 10 / 16 / 8 / 0 の
   自動判定，`0x` 前置 (後ろに数字が無い場合は `0` で止まる)，`endptr`，
   数字なし，`LONG_MAX` / `LONG_MIN` への飽和，基数 36。`abs` の正負，
   `div` の負の被除数・除数 (0 方向切捨て)
4. **リンクの単位**: 従来どおり `stdlib.o` 単独でリンクが通ること
