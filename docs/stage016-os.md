# Stage 16: 実行環境の拡張

本書は Stage 16 の設計文書である。1〜4 章は**この段を挟むと決めた
経緯** (GCC へ直行しない理由) で，5 章以降が設計である。

[roadmap.md](roadmap.md) 2.1 は分岐点を 3 案として提示し，「その時の
実装体力を見て決める」として保留していた。Stage 15 が終わった今が
その時である。

## 1. 前提が変わった: tcc が鎖の成果物になった

roadmap 2.1 を書いた時点で我々が持っていた処理系は cc だけだった
(C89。GNU 拡張は持たない)。**Stage 15 で tcc が加わった**
([stage015-tcc.md](stage015-tcc.md) 13 章)。

- T2 == T3 == tccH。tcc が我々の OS の上で自分自身を作り，その出力が
  動かない
- T2 は**参照実装が作る tcc とバイト一致**する
- tcc は鎖の成果物である。**Stage 16 の入力に使ってよい**
  ([artifacts.md](artifacts.md) の凍結規則の対象であり，外部バイナリの
  持ち込みには当たらない)

tcc は GNU C 拡張を相当量サポートする (statement expression・
`__attribute__`・`typeof`・computed goto・inline asm など)。**3 案の
どれを採るにせよ，「コンパイラ側」の負担は roadmap 執筆時より大幅に
軽い。**

## 2. しかし律速はコンパイラではなく OS である

GCC 4.7 系のビルドが要求するものと，我々の OS が今持っているものを
並べる。**左は実測値**である。

| | 我々の OS (実測) | GCC 4.7 のビルドが要るもの |
|---|---|---|
| ユーザ空間 | **14 MB** (`UBASE` 0x8600_0000 〜 `UBRKMAX` 0x86e0_0000) | cc1 が自分自身を翻訳するのに数百 MB |
| ファイル系 | sfs。既定 4 MiB (第 6 部で 8 MiB) | 展開後で数百 MB |
| 名前空間 | **平ら** (ディレクトリという実体を持たない) | 深いディレクトリ木を前提にする |
| 道具 | sh / ed / mk / bundle / ldin / eot | make・sh・awk・sed・tar，そして `configure` |
| シェル | 1 行 9 語・引数 8 個 | `configure` は数万行のシェルスクリプト |
| 依存ライブラリ | 無し | GMP・MPFR・MPC の 3 つが要る |

**桁が違う。** Stage 14 で bzip2 / zlib，Stage 15 で tcc を通せたのは，
どれも「単一ディレクトリ・数十ファイル・数 MB」に収まったからである。
GCC はそこから 2 桁上にある。

なお GCC 4.7.4 の実物はこの環境から取得できなかった
(`ftp.gnu.org` / `gcc.gnu.org` ともネットワークポリシーで拒否)。
上表の右列は構造的に確かな要求 (GMP/MPFR/MPC が要る，`configure` が
シェルスクリプトである，等) に留め，**数値の実測は取得できる環境で
行う**。

## 3. 3 案の再評価

roadmap 2.1 の 3 案を，1 と 2 を踏まえて評価し直す。

| 案 | 内容 | コンパイラ側 | OS 側 | RISC-V |
|---|---|---|---|---|
| A | C++ サブセット処理系を作り GCC 7 をビルド | **特大** (C++ を自作) | 特大 | 保てる |
| B | x86-64 backend を足し GCC 4.7 → 新しい GCC と登る | 中 (tcc がある) | 特大 | **主対象でなくなる** |
| C | GCC 4.7 に RISC-V backend を自作して載せる | 中 (tcc がある) | 特大 | 保てる |

**どの案でも OS 側の負担は変わらない。** つまり 2 の差は分岐点の前に
ある共通の作業であり，roadmap 2.1 が「どの案でもクリティカルパスは
共通」と書いたのと同じ構図が，もう一段先で繰り返されている。

コンパイラ側だけを見れば，Stage 15 の成果により **B と C の差は
「backend を書く対象が x86-64 か RISC-V か」だけ**に縮んでいる。
そして我々は第 5 部で **tcc に RV32 backend を書いた実績**がある
([stage015-riscv32.md](stage015-riscv32.md))。GCC の machine
description は tcc の backend より遥かに大きいが，「backend を書く」
という作業自体は未知ではない。

## 4. 提案: 分岐の前に OS を育てる段を挟む

2 の表から出る結論は「**GCC の前に，大きなビルドが成立する OS が要る**」
である。具体的に足りないものは次のとおりで，どれも A/B/C と独立に要る。

1. **記憶域**: ユーザ空間 14 MB → 数百 MB。ページングか，少なくとも
   もっと広い平坦空間
2. **ファイル系**: 平らな sfs → ディレクトリを持つもの。数百 MB
3. **道具**: awk 相当・sed 相当・本物の make。`configure` を通すなら
   POSIX シェルに近いものも
4. **依存ライブラリ**: GMP・MPFR・MPC (これ自体が Stage 14 規模の
   外部ソース 3 本である)

Stage 14 (bzip2 / zlib) と Stage 15 (tcc) が「外部ソースを 1 本ずつ
当てて処理系を鍛える」やり方で成功しているので，同じやり方を OS 側に
当てるのが素直である。**GCC より小さく，かつ上の 1〜4 を要求する
外部ソース**を次の標的に選ぶ。

## 5. 決めたこと

**OS を育てる段を Stage 16 として挟む。** 案 A/B/C の判断はその後へ
持ち越す (4 のとおり，どの案でも等しく要る作業だから，先延ばしの損が
無い)。GCC は Stage 17 になる。

手当ての順は**ファイル系 (ディレクトリ) を先**にする。理由は 3 つ。

1. 外部ソースは例外なく深いディレクトリ木を前提にする。平らなままでは
   **展開すらできない**。Stage 14 / 15 が通ったのは，どれも単一
   ディレクトリに収まったからである
2. 記憶域の不足は「容量が足りない」という分かりやすい症状で出る。
   第 6 部で何度も当たって都度広げてきた実績があり，後追いできる
3. ファイル系は自作の範囲で完結する。外部ソースの取得を要さない

## 6. 第 1 部の設計: ディレクトリを持つファイル系 (sfs2)

### 6.1 いまの sfs とその限界

[stage012-os.md](stage012-os.md) 4.3 の sfs は次の形である。

- 先頭に表 (エントリの配列)，その後ろにデータ
- エントリは「名前・オフセット・長さ」だけ
- **名前は平ら**。ホスト側の `find . -type f` が出す `a/b/c.h` という
  文字列を，そのまま 1 個の名前として持つ (ディレクトリという実体は
  無い)

平らでも「`a/b/c.h` を開く」ことはできる。実際 Stage 15 第 6 部では
`sys/time.h` をこの形で置いた。**足りないのは次の 3 つ**である。

| 足りないもの | なぜ要るか |
|---|---|
| 相対パスの解決 | `#include "../foo.h"` や，作業ディレクトリを持つビルド |
| 一覧 (readdir) | `configure` や make が「そこに何があるか」を訊く |
| 作成・削除 | ビルドは中間ディレクトリを掘る |

### 6.2 方針

**平らな表は残し，その上に木を載せる。** エントリに「親」と「種別」を
足すだけにする。sfs の「表 + データ」という単純さは，カーネルが
数百行で読めることの理由なので崩さない。

- エントリに **種別** (ファイル / ディレクトリ) と **親のエントリ番号**
  を足す
- 名前は**そのディレクトリ内での名前**にする (`c.h`)。全体の経路は
  親を辿って組み立てる
- ルートはエントリ 0 とし，親を自分自身にする

これで readdir は「親が n のエントリを並べる」，パス解決は「/ で切って
1 段ずつ引く」で済む。

### 6.3 バイト配置 (sfs2)

sfs1 の表項目はちょうど 64 バイトで隙間が無い。**名前を 52 → 48 に
縮めて，空いた 4 バイトを親に充てる。** 名前はそのディレクトリ内での
名前になるので，47 バイトあれば実用上足りる (sfs1 は経路まるごとを
名前にしていたので 51 バイトでも足りないことがあった)。項目が 64 バイト
のままなので，カーネル側の算術は変わらない。

すべてリトルエンディアン。

```
スーパーブロック (32 バイト):
  0   'sfs2' (4 バイト)
  4   u32 total    イメージ全体の大きさ
  8   u32 tbloff   表の位置 (= 32)
  12  u32 tblcnt   表の件数 (容量)
  16  u32 cursor   次にデータを置く位置 (追記割付け。4 バイト境界)
  20  予約 (0)

表項目 (64 バイト / 件。未使用の項目は全 0):
  0   name[48]     そのディレクトリ内での名前，NUL 終端 (最長 47 バイト)
  48  u32 parent   親のエントリ番号
  52  u32 dataoff
  56  u32 len
  60  u32 flags    bit0 = 有効, bit1 = ディレクトリ
```

- **エントリ 0 は必ずルート**である。名前は空，親は自分自身 (0)，
  flags = 3 (有効 + ディレクトリ)
- ディレクトリのエントリは `dataoff` / `len` を使わない (0)
- 経路の解決は「`/` で切り，1 段ずつ『親が n で名前が一致する項目』を
  引く」。一覧 (readdir) は「親が n の項目を並べる」

### 6.4 進め方

**ホスト側から先に作る。** `tools/sfs2.sh` (pack / unpack / list) と
その往復検査は QEMU を要さないので速く回せる。形式が固まってから
カーネルへ持ち込む。

sfs1 (`tools/sfs.sh`) は**そのまま残す**。Stage 12〜15 の検査がすべて
それに依っており，凍結済みの成果物の再現に要るからである。

### 6.5 カーネル側 (kernel17)

`stage016/kernel17.c` は kernel16 の写しに sfs2 の経路解決を入れたもの
である。触ったのは 3 関数だけで，残りは kernel16 のままである。

| 関数 | kernel16 | kernel17 |
|---|---|---|
| `nameeq` → `nameeqn` | 項目の名前と文字列全体を比べる | 項目の名前が `s[0, n)` と一致し**そこで終わる**か |
| (新設) `childof` | — | 親 `par` の中から名前 `s[0, n)` を引く |
| (新設) `walk` | — | 経路を `/` で切って 1 段ずつ辿る |
| `sfsfind` | 表を線形に走査して名前で引く | `walk(path, 0, ...)` |
| `sfsnew` → `sfsmk` | 空き項目に名前を書く | 親を `walk(path, 1, ...)` で出してから書く。ディレクトリも作れる |

`nameeqn` が「切り出さずに比べる」形なのは，**カーネルに経路の一時
領域を持たせないため**である。`a/b/c.h` の `b` を比べるのに `b` を
どこかへ複製する必要がない。

`walk` の `stop` 引数が 2 つの用途を 1 つの関数にまとめている。

- `stop = 0` (引く): 最後の段まで辿り，その項目番号を返す
- `stop = 1` (作る): 最後の段の**手前**まで辿り，その親を返す。最後の
  段の名前と長さを `*lastp` / `*lastn` で返す

**ファイルを途中に挟む経路は失敗させる** (`/top.txt/x`)。`walk` は
1 段進むごとに「まだ後ろが残っているのに今の項目がディレクトリでない」
かを見る。これが無いと `/top.txt/x` が `/top.txt` として開けてしまい，
「無いはずのものが開ける」という一番たちの悪い壊れ方をする。

連続する `/` と先頭の `/` は読み飛ばす (`//src///one.c` は
`/src/one.c` と同じ)。`.` と `..` はまだ扱わない —— 作業ディレクトリを
持つのは第 2 部の課題である。

検査は `tests/stage016/user/pathprobe.c` を OS の上で走らせて，10 件の
経路の引き方を期待と突き合わせる。中身をその経路の名札にしてあるので，
取り違えれば**どれとどれを取り違えたか**が出力に出る。

### 6.6 実測

`kernel17.bin` は 26,020 バイト (kernel16 との差は経路解決のぶんだけ)。
`pathprobe` を kernel17 の上で走らせた結果は下のとおりで，10 件すべてが
期待と一致した。

| 名札 | 経路 | 期待 |
|---|---|---|
| `abs` | `/top.txt` | `TOP` |
| `rel` | `top.txt` | `TOP` |
| `deep` | `/src/a/b/three.c` | `THREE` |
| `dup-inc` | `/inc/one.c` | `INC-ONE` |
| `dup-src` | `/src/one.c` | `SRC-ONE` |
| `slashes` | `//src///one.c` | `SRC-ONE` |
| `miss-top` | `/nosuch.txt` | 開けない |
| `miss-deep` | `/src/nosuch.c` | 開けない |
| `thru-file` | `/top.txt/x` | 開けない |
| `miss-dir` | `/nodir/one.c` | 開けない |

`dup-inc` と `dup-src` が別物として引けたことが第 1 部の目的そのもので
ある。sfs1 では**この 2 つは同じ名前を持てなかった**ので，木の中に
同名のファイルを置くこと自体ができなかった。

第 1 部の検査は sfs2 のホスト側 9 件と合わせて 11 件で，すべて通っている。

## 7. 第 2 部の設計: ディレクトリを操作する

第 1 部で経路は引けるようになったが，**木を見る手段も，木を作る手段も，
木の中で位置を持つ手段も無い**。`configure` と make はこの 3 つを前提に
している。第 2 部でそれを入れる。

### 7.1 何が足りないか

| 足りないもの | 誰が要求するか |
|---|---|
| 一覧 (`opendir` / `readdir`) | `configure` が「そこに何があるか」を訊く。make のワイルドカード |
| 作成 (`mkdir`) | ビルドが中間ディレクトリを掘る |
| 作業ディレクトリ (`chdir` / `getcwd`) | `cd src && make` の形。相対経路のすべて |
| `.` と `..` | 相対経路の中に必ず現れる (`../include`) |

### 7.2 syscall

番号は引き続き RV32 Linux 互換にする。ここも実 Linux でそのまま通じる
形にしておくと，L2 (Linux をビルドする) への経路が保てる。

| 番号 | 名前 | 用途 |
|---|---|---|
| 17 | `getcwd` | 作業ディレクトリの経路を書き出す |
| 34 | `mkdirat` | ディレクトリを作る (RV32 に `mkdir` は無い) |
| 49 | `chdir` | 作業ディレクトリを移す |
| 61 | `getdents64` | ディレクトリの項目を読み出す |

`getdents64` が返す 1 件の形も Linux に合わせる。可変長で，`d_reclen`
だけ進めば次の件に着く。

```
  0   u64 d_ino      項目の索引 (sfs2 のエントリ番号)
  8   s64 d_off      次の件の読み出し位置
  16  u16 d_reclen   この件の全長 (8 バイト境界へ切上げ)
  18  u8  d_type     4 = ディレクトリ, 8 = ふつうのファイル
  19  char d_name[]  NUL 終端の名前
```

64 bit の欄は 32 bit の語 2 本として書く。カーネルは 32 bit なので，
上位は常に 0 である。

### 7.3 作業ディレクトリ

カーネルは `cwd` を 1 つだけ持つ (エントリ番号。初期値 0 = ルート)。
プロセスは一度に 1 つしか走らないので，これで足りる。

経路の解決は第 1 部の `walk` をそのまま使い，**出発点だけを変える**。

- `/` で始まる → ルート (エントリ 0) から
- そうでない → `cwd` から

段の名前が `.` なら動かず，`..` なら親へ上がる。**ルートの親はルート
自身**なので `/..` は `/` になる (Linux と同じ)。`..` はディレクトリ
以外には現れないので，`walk` の「ファイルを途中に挟む経路は失敗」の
検査はそのまま効く。

`getcwd` は逆向きに辿る。`cwd` から親を辿ってエントリ番号を積み，
逆順に名前を並べる。sfs2 は親を持っているのでこれができる
(sfs1 にはそもそも親が無かった)。

### 7.4 libc が邪魔をしていた箇所

libc15 の `open` は**先頭の `/` の並びを全部剥がしていた**。

```c
  /* ルート直下しか無いので，先頭の '/' の並びは剥がして裸の名前にする */
  while (*path == '/')
    path = path + 1;
```

平らな名前空間では正しかった。しかし**作業ディレクトリを持った瞬間に
これは重大な誤りになる** —— `/inc/one.c` が `inc/one.c` になり，
ルートからではなく `cwd` から引かれてしまう。「絶対経路を渡したのに
今いる場所によって別のファイルが開く」という，第 1 部の
`thru-file` と同じ種類の (黙って間違う) 壊れ方である。

剥がす処理は**要らなくなっている**。この処理が入った理由は tcc が
`-I/` から `//tccdefs.h` の形の経路を作ることだったが，第 1 部の
`walk` は先頭と連続の `/` を読み飛ばすので，そのまま渡して正しく
引ける (`slashes` の検査がそれを見ている)。libc16 では削る。

### 7.5 世代

[artifacts.md](artifacts.md) 5 章の手順どおり，直前の世代を全文複製して
から編集する。

| 新しい世代 | 複製元 | 変えるところ |
|---|---|---|
| `stage016/kernel18.c` | `stage016/kernel17.c` | `cwd`・`.` / `..`・4 つの syscall・ディレクトリを `open` できるようにする |
| `stage016/libc/` | `stage015/libc/` | `dirent.h`・`sys/stat.h`・`chdir` / `getcwd`・7.4 の削除 |

kernel17 は第 1 部の成果物として残す。第 1 部の検査 (`pathprobe`) は
kernel17 の上で走り続けるので，**第 2 部の変更が第 1 部を壊していない
ことが常に見える**。

### 7.6 実測

`dirprobe` を kernel18 の上で走らせた結果は 31 件すべて期待と一致した。
第 1 部と合わせて Stage 16 の検査は 13 件 (sfs2 のホスト側 9・kernel17
の経路解決 2・kernel18 のディレクトリ操作 2) で，すべて通っている。

要点は次の 3 つである。

| 名札 | 見ていること |
|---|---|
| `abs-from-src` | cwd が `/src` のときに `/inc/one.c` が `INC-ONE` を返す。**libc が絶対経路を相対に変えていない**こと (7.4) |
| `dotdot2` | `/src/a` から `../../inc/one.c` が引ける。`..` が 2 段続けて上がれること |
| `read-dir` | ディレクトリの fd への `read` が `-1` になる。fdpos の意味が二重になっているのを黙って通さないこと |

### 7.7 見つかった「黙って間違う」実装 2 件

第 2 部の作業の中身は，syscall を足すことよりも**平らな名前空間の
時代に正しかった実装が，木を持った途端に嘘になる**箇所を見つけて
潰すことだった。2 件あり，どちらも同じ形をしている。

| 場所 | 平らなときは | 木を持つと |
|---|---|---|
| `posix/sys.c` の `open` が先頭の `/` を剥がす | ルート直下しか無いので正しい | 絶対経路が `cwd` から引かれる |
| `src/misc15.c` の `getcwd` が常に `"/"` を返す | 常にルートにいるので正しい | どこにいても `/` と答える |

`getcwd` のほうは**リンクの多重定義 (ld の終了コード 3) で偶然見つかった**。
本物を `posix/sys.c` に置いたら，純粋部に同じ名前の偽物が残っていた
のである。検査で見つけたのではない点は記録しておく価値がある ——
**「値を返すが嘘である」実装は，呼び手が正しく動いてしまうので
検査を書かない限り永久に見つからない。**

### 7.8 残した穴

`src/misc15.c` の `unlink` / `remove` は「何もせず 0 を返す」ままである。
これも上の 2 件と同じ *bad* の形だが，直すには sfs2 に削除が要る。
第 3 部の課題とする。

削除を入れるときは追記割付け (6.3 のカーソル) との折り合いが要る。
項目を消しても穴が空くだけで詰まらないので，「消した項目を再利用する」
か「詰め直す」かを決めることになる。

## 8. 第 3 部の設計: 記憶域を広げる

### 8.1 いまの壁は 14 MB である

ユーザプログラムが `malloc` で取れる上限は

```
UBRKMAX - UBASE = 0x86e0_0000 - 0x8600_0000 = 14 MB
```

から像の大きさを引いたぶんである。tcc の自己ホスト (Stage 15) はここに
収まったが，GCC のビルドは数百 MB を要る。**これは工夫で回避できる
種類の不足ではない。**

### 8.2 何が動かせて，何が動かせないか

配置定数はカーネルの `#define` だけにあるわけではない。**リンカの
前置部に機械語として焼き込まれているものがある** (`stage015/ld16.sc`)。

| 値 | どこに焼かれているか | 動かせるか |
|---|---|---|
| トラップフレーム `0x8370_0000` | `prologuek` の `lui t6, 0x83700` | **不可** (ld の世代が要る) |
| カーネルスタック `0x8380_0000` / `0x83f0_0000` | `prologuek` の `lui sp` / `lui s1` | **不可** (同上) |
| ユーザ像のロード先 `0x8600_0000` | `ld` の `base`。`'E'` 形式の既定 | **不可** (同上) |
| `UBRKMAX` / `USP` | カーネルの `#define` のみ | 可 |
| `SAVEA` / `SAVETOP` | カーネルの `#define` のみ | 可 |
| QEMU の RAM 量 | `tools/run-qemu.sh` の `-m` | 可 |

したがって **`0x8600_0000` より下は 1 バイトも動かさない**方針を採る。
そうすればリンカの世代を刻まずに済み，凍結済みの `.sc` と，そこから
出る全成果物の SHA-256 がそのまま生きる。**広げるのは上だけである。**

### 8.3 新しい配置 (RAM を 512 MB にする)

```
  0x8000_0000  カーネル                       ← 変えない
  0x8370_0000  トラップフレーム               ← 変えない (ld16 が持つ)
  0x8380_0000  カーネルスタック               ← 変えない (ld16 が持つ)
  0x8400_0000  sfs2 イメージ (32 MB)          ← 変えない
  0x8600_0000  ユーザ像 (UBASE)               ← 変えない (ld16 の 'E' base)
       …       .text/.data/.bss + データスタック 256 KiB
       …       ヒープ (brk がここから上へ伸びる)
  0x9600_0000  UBRKMAX                        ← 0x86e0_0000 から移す
       …       フレームスタック (16 MB)
  0x9700_0000  USP                            ← 0x8700_0000 から移す
  0x9700_0000  spawn の退避領域 (144 MB)      ← 0x8100_0000 から移す
  0xa000_0000  RAM の終わり (-m 512M)
```

ヒープの上限は **0x9600_0000 - 0x8600_0000 = 256 MB** になる (像と
データスタックのぶんを引く)。14 MB から 18 倍である。

`0x8100_0000`〜`0x8370_0000` は退避領域を移した跡が空く。**当面は
使わない**。sfs2 を 32 MB より大きくしたくなったときの行き先にする。

### 8.4 退避領域と釣り合い

`spawn` は親の像とフレームスタックを丸ごと退避領域へ写す
([stage013-tools.md](stage013-tools.md) 3.2)。したがって

```
退避領域の大きさ ≧ 親が使っている記憶域 × 入れ子の深さ
```

でなければ `spawn` が通らない。上の配置では**ヒープが 256 MB 取れる
のに退避領域は 144 MB** なので，記憶域を大きく使った親からの `spawn`
は失敗する。

これは意図した釣り合いである。理由は 2 つ。

- カーネルは既に `savecur + 296 + imgsz + stksz > SAVETOP` を検査して
  `ENOMEM` を返す。**黙って壊れるのではなく，はっきり失敗する** ——
  台帳でいう gap であって bad ではない
- 記憶域を最も使うのはコンパイラ本体であり，それは `make` から
  `spawn` される**子**の側である。子は入れ子の末端なので，退避される
  ことがない

親 (シェルや make) が数百 MB を使い始めたら，そのときに配置を見直す。

### 8.5 世代

カーネルだけが変わる。[artifacts.md](artifacts.md) 5 章のとおり
`stage016/kernel18.c` を全文複製して `stage016/kernel19.c` を作り，
`#define` の 4 つを変える。**リンカも libc も変えない。**

`tools/run-qemu.sh` の `-m` と，検査が作る RAM ファイルの大きさも
128 MB から 512 MB へ揃える。RAM ファイルは疎ファイルなので，実際に
消費するのは QEMU が触れたページのぶんだけである。

### 8.6 実測

同じ `memprobe` を 2 つのカーネルで走らせて対にした。**片方だけを
見ても「広がった」ことは言えない**ので，検査は必ず両方を出す。

| カーネル | RAM | 取れた量 |
|---|---|---|
| kernel18 (第 2 部) | 128 MB | **13 MiB** |
| kernel19 (第 3 部) | 512 MB | **255 MiB** |

13 MiB は `UBRKMAX - UBASE = 14 MB` から像とデータスタック (256 KiB)
を引いた値で，計算と合う。255 MiB も同様である。

`memprobe` は `sbrk` の返り値を信じない。**1 MiB 取るごとにその先頭と
末尾へ印を書き，最後にもう一度なめて読み戻す。** カーネルの `UBRKMAX`
だけを上げて QEMU の RAM が足りていなければ「確保は成功するのに書いた
瞬間に落ちる」ことになり，それは台帳でいう bad だからである。
`verify ok` はその 2 度目のなめが通ったことを表す。

### 8.7 既定の RAM 量は変えていない

`tools/run-qemu.sh` に `STONE_QEMU_RAM` を足したが，**既定は 128M の
ままにした**。kernel15〜18 は 128 MB を前提にした配置を持っており，
それらの検査は当時と同じ大きさで再現できるほうがよい。広い記憶域を
要るのは kernel19 以降だけなので，**要る側が明示的に指定する**形にした。


## 9. 第 4 部の設計: 道具立て (configure が通る Unix)

### 9.1 完了条件を実物で決める

「道具立て」は際限なく広げられるので，**具体的なスクリプト 1 本が
そのまま走ること**を完了条件にする。対象は tcc の `configure`
(768 行) にした。理由は 3 つ。

- **既に手元にある。** 作業環境の網は制限されており，新しい tarball を
  取ってこられない (`ftp.gnu.org` は 403)。鎖が既に読んでいる資産の中
  から選ぶしかない
- **他人が書いた本物の POSIX シェルスクリプトである。** 我々が
  「通るように」書いたものではない
- **答え合わせができる。** ホストの `sh` で走らせた結果 (`config.h` /
  `config.mak`) と突き合わせれば，通ったかどうかが機械的に決まる

### 9.2 何が要るか (数えた結果)

`configure` が使っている機能を数えた。

| 機能 | 回数 | 機能 | 回数 |
|---|---|---|---|
| パイプ `\|` | 74 | `case` | 19 |
| `test` / `[` | 72 | `for` | 11 |
| `if` | 51 | `tr` | 9 |
| `echo` | 31 | `grep` / `cat` | 4 / 4 |
| `$(...)` / `` ` `` | 22 / 8 | `eval` | 3 |
| | | `rm` / `sed` / `mkdir` / `while` | 2 / 1 / 1 / 1 |

**`awk` は 1 度も出てこない。** text 処理系のうち最も重いものが要件から
外れる。`sed` も 1 箇所だけである。

いまの `sh` (`stage013/sh.c`，73 行) が持っているのは「語に分割して
`spawn`」と `<` `>` だけなので，上の表はほぼ全部が不足である。

### 9.3 パイプをどう作るか (プロセスが 1 つしかない)

カーネルは**プロセスを一度に 1 つしか走らせない**。`spawn` は子の
終わりを待つ逐次実行である ([stage013-tools.md](stage013-tools.md) 3.2)。
本物のパイプは 2 つのプロセスが同時に走ることを要求するので，このまま
では作れない。

選べるのは 2 つ。

| 案 | 内容 | 得失 |
|---|---|---|
| P1 | カーネルに並行実行を入れる | 本物。スケジューラ・待ち行列・fd の共有が要る。**カーネルの作り直しに近い** |
| P2 | 一時ファイルで中継する | 左を一時ファイルへ流し切ってから右を走らせる。逐次実行のまま作れる |

**P2 を採る。** 違いが出るのは「無限に流れる左」と「早く打ち切る右」
(`yes | head -1`) だけで，`configure` にはどちらも現れない。P1 は
それ自体が Stage 1 つぶんの仕事であり，**`configure` を通すという
目的に対して釣り合わない**。

代償ははっきり書いておく。**P2 のパイプは左を全部走らせてから右を
始めるので，左が終わらなければ右は始まらない。** これは gap であって
bad ではない (黙って違う答えを出すのではなく，単に止まる)。

`$(...)` と `` ` `` も同じ仕掛けで作れる —— 中身を一時ファイルへ流し，
読み戻して末尾の改行を落とす。

### 9.4 一時ファイルには削除が要る (第 2 部で残した穴)

P2 は一時ファイルを大量に作る。したがって **sfs2 に削除を入れる**
必要がある。7.8 で第 3 部送りにした `unlink` / `remove` の穴が，ここで
塞がれることになる。

削除は追記割付け (6.3 のカーソル) と噛み合わせる必要がある。項目を
消しても穴が空くだけで詰まらないからである。決め方は 2 つある。

| 案 | 内容 |
|---|---|
| D1 | 項目を未使用に戻すだけ。データの穴は放置し，カーソルは進む一方 |
| D2 | 消すたびに後ろを詰める |

**D1 を採る。** D2 は全項目の `dataoff` を書き換えることになり，開いて
いる fd が指す位置がずれる。D1 の代償は「作っては消すを繰り返すと
イメージを使い切る」ことだが，これも**はっきり `ENOSPC` で失敗する**
ので bad ではない。`configure` 1 回ぶんの一時ファイルは 32 MB の
イメージに十分収まる。

### 9.5 分け方

1 本の PR には大きすぎるので 3 つに割る。

| 部 | 中身 |
|---|---|
| 第 4 部の 1 | sfs2 の削除 (`unlink`)。カーネルと libc。**一時ファイルの土台** |
| 第 4 部の 2 | シェル (`sh2`)。引用符・変数・`test`・`if`/`while`/`for`/`case`・`&&`/`\|\|`・パイプ・`$(...)`・`eval` |
| 第 4 部の 3 | 中核の道具 (`echo` / `cat` / `rm` / `mkdir` / `tr` / `grep` / `sed`) と，`configure` を実際に走らせた突き合わせ |

(第 4 部の 3 の道具の並びは 11.1 で訂正した。`tr` と `sed` は数え
間違いで，実際に要るのは `uname` / `grep` / `cat` / `rm` / `mkdir` /
`head` / `diff` / `mv` / `cp` / `ln` の 10 個である。)

順序に意味がある。**削除が無いとパイプが作れず，パイプが無いと
`configure` は 1 行目から進まない。**

### 9.6 数え直し: シェルの要件はもう一段深い

9.2 は「1 行に現れる字面」を数えたものだったので，**構文の深いところを
数え落としていた**。実装に入る前に数え直した結果を残す。

| 要る機能 | 回数 | 9.2 で見えていたか |
|---|---|---|
| **シェル関数の定義** | 11 | 見落とし |
| `${V#*=}` `${V%%=*}` `${V%eabi}` (接頭・接尾の切り落とし) | 8 | 見落とし |
| `${V:-既定}` `${V:=既定}` (既定値) | 3 | 見落とし |
| **here-doc** (`cat << EOF`) | 3 | 見落とし |
| `set --` (位置パラメータの置き換え) | 2 | 見落とし |
| `${V}` (素の展開) | 32 | ○ |
| `$1` `$2` `$3` `$#` `$?` | 46 | ○ |
| `unset` / `local` | 1 / 1 | 見落とし |

関数名は `assign_opt` / `good_split` / `confvars_has` / `confvars_set` /
`show_help` / `default` / `default_conf` / `fcho` / `print_str` /
`print_num` / `fn_makelink` の 11 個である。

**とくに重いのは関数とパラメータ展開の演算子**で，どちらも「あれば
便利」ではなく，無いと `configure` が 1 行目から進まない種類のもので
ある (69 行目が `set -- "${2:-${1%%=*}}" "${1#*=}"` で，ここに 3 つ
同時に出てくる)。

`local` は POSIX ではないが 1 箇所で使われている。関数内の変数を
局所にするだけなので，関数を実装するなら付いてくる。

**この数え直しを記録に残すのは，9.2 の数え方が甘かったからである。**
字面を数えると「パイプ 74・test 72」のような目立つものは拾えるが，
関数定義や `${V#...}` のような**構文そのもの**は落ちる。要件を数える
ときは字面と構文を別々に数える必要がある。

### 9.7 第 4 部の 1 の実測

`rmprobe` を kernel20 + libc17 の上で走らせ，24 件すべてが期待と
一致した。Stage 16 の検査は 20 件で全通過である。

**検査を書いたおかげで設計と実装の食い違いが 1 つ出た。** 9.4 で
「開いている fd からは読み書きできる」と書いたのに，実装は `unlink` で
`E_LEN` を 0 にしていた。`read-unlinked` が落ちて分かった。

直し方は `F_DEL` (bit2) を足すことである。開いている fd が指している
項目を消すときは，

- 名前と親だけ消す (経路からは引けなくなる)
- 実体 (`dataoff` / `len`) は残す
- **`F_USED` は落とさない**

とし，最後の fd が閉じたときに本当に解放する。

**`F_USED` を落とさないのが要点である。** 落とすと `sfsmk` が同じ項目を
再利用でき，まだ読んでいる fd の下で別のファイルが割り当たって，
**黙って別の中身が読める**ことになる。台帳でいう bad であり，しかも
「一時ファイルを作っては消す」という第 4 部の 2 の主要な使い方で
ちょうど起きる。

`getdents64` も `F_DEL` を除く必要があった。親を 0 にしてあるので，
除かないと**ルートの一覧に空の名前で現れる**。

### 9.8 Stage 16 で見つけた「黙って間違う」実装

第 1〜4 部を通して 5 件あった。**すべて「平らな名前空間の時代には
正しかった」という同じ形をしている。**

| 世代 | 場所 | 木を持つと何が嘘になるか |
|---|---|---|
| libc16 | `open` が先頭の `/` を剥がす | 絶対経路が `cwd` から引かれる |
| libc16 | `getcwd` が常に `"/"` | どこにいても `/` と答える |
| libc17 | `unlink` が何もせず 0 | 消したと言ったのに消えていない |
| libc17 | `realpath` が複写するだけ | 相対経路がそのまま返る |
| kernel20 | `unlink` が `E_LEN` を 0 | 開いている fd が読めなくなる |

見つかり方は 3 通りで，**検査で見つかったのは 5 件中 2 件だけ**である。

| 見つかり方 | 件数 |
|---|---|
| 検査が落ちた | 2 (`realpath` は書く前に気づいた・`unlink` の `E_LEN`) |
| リンクの多重定義 | 1 (`getcwd`) |
| 実装中に読んで気づいた | 2 (`open` の `/` 剥がし・`unlink` の偽物) |

**値を返すが嘘である実装は，呼び手が正しく動いてしまう。** だから
「動かして確かめる」では見つからない。見つけるには，前提が変わった
ときに**前提に依っていた実装を洗い出す**しかない。Stage 16 の第 1 部で
「名前空間が平らでなくなった」と決めた時点で，`stage*/libc/` を
`grep` して「ルート直下」「平ら」と書いてあるコメントを全部読み直す
べきだった。

## 10. 第 4 部の 2 の設計: シェル (sh2)

### 10.1 要件の確定

9.2 と 9.6 で数えたものに，実際の経路を読んで分かったものを足す。

| 要る | 根拠 |
|---|---|
| 語の分割・引用符 (`'` `"` `\`) | 全域 |
| 変数の代入と展開，`${V}` | 32 箇所 |
| **パラメータ展開の演算子** `#` `%` `%%` `:-` `:=` | 11 箇所 |
| 位置パラメータ `$1`..`$9` `$#` `$?`，`set --` | 48 箇所 |
| **シェル関数**と `local` | 11 個 |
| `if` / `while` / `for` / `case` | 82 箇所 |
| `case` のパターン照合 (`*` `?` `[...]` `\|`) | 19 箇所 |
| `&&` `\|\|` `;` | 全域 |
| パイプ | 74 箇所 |
| `$(...)` と `` ` `` | 30 箇所 |
| リダイレクト `<` `>` `>>` `2>` と here-doc | 3 + α |
| `eval` (**動的な変数名への代入**を含む) | 3 箇所 |
| **IFS による語分割** (引用しない展開) | `good_split` が依存 |
| 組込み `test` / `[` | 72 箇所 |

**要らないと分かったもの**も書いておく。要らないと分かっているものを
作らないことが，この規模では効く。

- **経路展開 (pathname glob)**。`*` は `case` のパターンと
  パラメータ展開にしか現れない。ディレクトリを走査する必要がない
- `awk`
- ジョブ制御，`&` による背景実行，サブシェルの並行
- 算術展開 `$(( ))` (現れない)

`eval ${1#--}=\$2` (70 行目) と `set -- "$1" $1` (75 行目) が要点である。
前者は**変数名を実行時に組み立てて代入**し，後者は**引用しない展開が
IFS で分割されること自体を検査に使っている**。どちらも「それらしく
動く」実装では通らない。

### 10.2 作り

素直な再帰下降にする。**構文木を作ってから歩く**ので，`eval` と関数が
自然に載る (どちらも「文字列を構文木にして歩く」だけになる)。

```
  list     := and_or ((';' | '\n') and_or)*
  and_or   := pipeline (('&&' | '||') pipeline)*
  pipeline := command ('|' command)*
  command  := simple | if | while | for | case | '{' list '}' | funcdef
  simple   := (assign | word | redirect)*
```

実行は木を歩くだけである。段ごとの決め方は次のとおり。

| 段 | 決め方 |
|---|---|
| パイプ | 左を一時ファイルへ流し切ってから右を走らせる (9.3) |
| `$(...)` | 同じ仕掛け。読み戻して末尾の改行を落とす |
| 関数 | 名前 → 構文木の表。呼ぶときは位置パラメータを退避して差し替える |
| `local` | 関数に入るときに変数表の丈を覚え，出るときに戻す |
| `case` | パターン照合は再帰で書く (`*` があるので後戻りが要る) |

### 10.3 語の展開の順序

POSIX の順序をそのまま守る。**順序を崩すと `good_split` が通らない。**

1. パラメータ展開 (`$V` / `${V}` / `${V#...}` …)
2. コマンド置換 (`$(...)` / `` ` ``)
3. **語分割** —— 引用の外から来た部分だけを IFS で切る
4. 引用の除去

3 が「引用の外から来た部分だけ」であることが肝である。`"$1"` は
切らないが `$1` は切る。したがって**展開の結果に「この文字は引用の
中から来たか」の印を持ち歩く**必要がある。語を `char *` ではなく
「文字と印の対」の列として扱う。

### 10.4 世代

`stage016/sh2.c` を新しく作る (`stage013/sh.c` は 73 行の別物なので
複製ではなく新規)。libc は第 17 世代をそのまま使う。カーネルは
kernel20 のままでよい —— パイプもコマンド置換も一時ファイルなので，
新しい syscall は要らない。

### 10.5 標準エラーのつなぎ替え (kernel21 / libc18)

`sh2` を書いて `configure` に当てたところ，**最後に 1 行だけ差が残った**。

```
configure 725 行:  diff $TMPH config.h >/dev/null 2>&1
```

カーネルは `spawn(path, argv, in, out)` の 4 つしか受けず，**fd 2 の
つなぎ替えを持たない** ([stage013-tools.md](stage013-tools.md) 3.2)。
そのため `diff` の「そんなファイルは無い」が端末へ漏れた。

`configure` は `2>/dev/null` (捨てる) と `2>&1` (集める) の両方を使う。
どちらも「標準エラーの行き先を決める」ことなので，カーネルに欄を
1 つ足して塞ぐ。

| 何 | どうした |
|---|---|
| `kernel21` | `spawn2` (501) を足す。記録は 5 語で，5 語目が `err` |
| | `fd2ent` / `fd2pos` を持ち，`sys_write` が fd 2 を見る |
| `libc18` | `spawn2(path, argv, in, out, err)` |
| `sh2` | `2>file` を `R_ERR` として拾い，`2>&1` は `out` を `err` にも渡す |

**番号を分けたのが要点である。** 500 の記録は 4 語，501 は 5 語なので，
同じ番号にすると**古い呼び手が積んだ 4 語の記録の後ろをカーネルが
読んでしまう**。500 はそのまま残し，5 語目を見るのは 501 のときだけに
した。

退避レコードにも `fd2ent` / `fd2pos` を足した (+296 / +300)。子が
標準エラーをつなぎ替えても親は影響を受けない。

### 10.6 実測: configure が完全に一致した

```
config.h     一致
config.mak   一致
config.texi  一致
Makefile     一致
標準出力      一致
```

**差は 1 行も無い。** 第 4 部の完了条件 (9.1) を満たした。

ただし**これはホストの `gcc` で `sh2` を組んで確かめた結果である。**
鎖の上 (cc15p でコンパイルし，kernel21 の上で走らせる) でも同じに
なることの確認が残っている。ホストで先に詰めたのは，1 回の試行が
QEMU 経由より桁違いに速いからで，**そこで見つかる誤りの大半は
処理系に依らない論理の誤り**だからである。実際に見つかった 11 件は
すべてそうだった。

### 10.7 鎖の上での実測

ホストで詰めた `sh2` を，鎖の上でも確かめた。

| 段 | 結果 |
|---|---|
| `cc15p` で翻訳 | 通る。`sh2.bin` は 161,692 バイト |
| `kernel21` の上で実行 | **参照シェル (本物の POSIX sh) と完全一致** |

1,759 行が cc15p の器に収まり，`static` 関数・再帰下降の深さ・
構造体の配列のどれも問題にならなかった。**ホストで直した 11 件以外に，
処理系に起因する誤りは 1 件も出なかった。**

検査の作りに 1 つ決め事がある。**期待値は本物の POSIX シェルで作る。**

```
sh tests/stage016/user/shprobe.sh > expected/shprobe.txt
```

我々が書いた期待値と突き合わせると，**思い違いをそのまま固定して
しまう**。実際 9.2 の数え落とし (10.1) も，`"$@"` の特例 (10.6) も，
「そうだと思っていたこと」が違っていた例である。参照実装があるなら
それを正とするほうが強い。

検査は 3 件立てた。参照シェルで通ること，**記録した期待値が参照
シェルの出力と一致すること** (期待値が腐ったら気づく)，そして `sh2` が
`kernel21` の上で同じ結果を出すこと。

### 10.8 第 4 部の 2 の到達点

Stage 16 の検査は **23 件で全通過**。

| 何 | どこで | 結果 |
|---|---|---|
| tcc の `configure` (768 行) | ホストで組んだ `sh2` | 生成物 4 つ + 標準出力が**完全一致** |
| `shprobe.sh` | 鎖で組んだ `sh2` / `kernel21` | 参照シェルと**完全一致** |

**残っているのは「`configure` を我々の OS の上で走らせる」ことである。**
それには `configure` が呼ぶ外部の道具 (`tr` / `grep` / `cat` / `rm` /
`mkdir` / `sed`) が OS の側に要る。第 4 部の 3 の課題とする。

## 11. 第 4 部の 3 の設計: 外部の道具

### 11.1 訂正: 9.2 の `tr` と `sed` は数え間違いだった

9.2 で「`tr` 9 回・`sed` 1 回」と書いたが，**これは誤りである**。
`grep -o 'tr '` が

```
712:print_str CONFIG_TCC_SYSINCLUDEPATHS "$tcc_sysincludepaths"
       ^^^
221:  --tcc-switches=...   specify implicit switches passed to tcc
          ^^^
```

のように**語の中の文字列に当たっていた**。`configure` は `tr` も `sed`
も `awk` も呼んでいない。

数え直した (コマンドの位置に現れる語だけを取る)。

| 道具 | 回数 | 何に使うか |
|---|---|---|
| `uname` | 5 | `-m` `-r` `-o` `-p` と素。機種と OS の判別 |
| `cat` | 3 | here-doc をファイルへ / 補助の表示 |
| `grep` | 4 | `/proc/cpuinfo` の判別 2 件と，`cc_msg.txt` から警告を探す 2 件 |
| `rm` | 2 | 一時ファイルの後始末 |
| `mkdir` | 1 | `-p` |
| `head` | 1 | `VERSION` を読む |
| `diff` | 1 | `config.h` を書き換える必要があるかの判定 |
| `mv` | 1 | `-f` |
| `cp` / `ln` | 各 1 | `lib` / `tests` を張る (`fn_makelink`) |

**`tr` / `sed` / `awk` が要らない**のは大きい。3 つとも本体が大きく，
とくに `sed` と `awk` は独自の言語を持つ。

数え間違いの教訓を書いておく。**`grep -c '道具名 '` は語の中に当たる。**
コマンドとして呼ばれているかを見るには，行頭・`;`・`|`・`&&`・`` ` ``・
`$(` の直後という**位置**で絞る必要がある。9.6 で「字面と構文を別々に
数える」と書いたが，字面の数え方自体も甘かった。

### 11.2 組込みにするか，別のプログラムにするか

要るのは 10 個で，どれも中身は小さい。作り方が 2 つある。

| 案 | 内容 | 得失 |
|---|---|---|
| B1 | `sh2` の組込みにする | 実行形式が増えない。`spawn` も要らない。**シェル以外から呼べない** |
| B2 | それぞれ別のプログラムにする | 本物の Unix に近い。10 個ぶんのビルド配線と像の容量が要る |

**B1 を採る。** 理由は 2 つ。

- `configure` も `make` も**コマンドをシェル経由で起動する**ので，
  組込みでも外から見た振舞いは変わらない
- いまの作業環境ではビルド 1 回に 40 分かかり，10 個の実行形式を
  足すと配線と検証の往復がそのぶん増える。**得られるものに対して
  費用が見合わない**

代償ははっきり書く。**組込みはシェルを介さない呼び手からは使えない。**
将来 `make` を自作の実装に替えたとき，それが直接 `spawn` するなら
届かない。そのときは B2 へ移す —— 中身は既に書けているので，
`main()` を被せて別々にリンクするだけの機械的な作業になる。

**実測を足す。** `configure` を 1 回通したときに道具が呼ばれる回数を
数えた (`spawn` の側に記録を仕込み，組込みの引きにも印を付けた)。

| 呼び方 | 道具の呼出し | 外へ出た回数 |
|---|---|---|
| 木の外から (`sh2 /path/to/configure`) | 22 | 6 |

内訳は `ln` 5 / `uname` 4 / `mkdir` 4 / `rm` 2 / `grep` 2 / `cat` 2 /
`mv` 1 / `head` 1 / `diff` 1 である。11.1 の表 (字面を数えたもの) と
合わないのは，**字面の数と実行の数は別物**だからである。

- `grep` は 4 箇所あるが，2 箇所は `/proc/cpuinfo` を見る枝で，
  我々の OS にその擬似ファイルは無いので通らない
- `cp` は 0 回である。`ln` が成功する限り `cp` の行は走らない
- `mkdir` は表では 1 だが実行は 4 である。`fn_makelink` の中の
  1 行が 4 つのファイルについて回る

**そして呼び方で変わる。** 木の中で `sh2 configure` と呼ぶと
`source_path_used=no` になり，`ln` 5 回と `mkdir` 4 回が丸ごと
消えて 13 回になる。「何回呼ばれるか」に唯一の答えは無い。

外へ出た 6 回はすべてコンパイラ関係である (`gcc` 2 回，`gcc` の
作った `./conftest` 4 回)。**道具の側はもう外を必要としていない。**
残っているのは「コンパイラをコマンドとして持つ」ことだけである。

**組込みにした本当の理由も，ここで正しておく。** 速さではない。
22 回なら実行形式を分けても大した差は出ない。理由は `sh2` に
**PATH 探索が無い**ことである。`spawn2` に渡した名前がそのまま
パスになるので，`cat` と裸の名前で呼ばれたものを外部の実行形式
として起動するには，PATH 探索を入れるか，道具一式をビルド先の
作業ディレクトリへ配って回るかのどちらかが要る。10 個の小さな
関数を持たせるほうが安い。

### 11.3 `uname` が返す値

`uname` は**我々の OS の素性を答える**ので，中身を決めておく必要がある。

| 問い | 返す値 | 理由 |
|---|---|---|
| (素) | `stone` | 我々の OS の名前 |
| `-m` | `riscv32` | 機種 |
| `-r` | `16` | Stage の番号を版とする |
| `-o` | `stone` | Android かの判定に使われるだけ |
| `-p` | `riscv32` | 同上 |

`configure` はこれを見て `targetos` と `cpu` を決める。**`stone` は
`configure` の知らない名前なので，既定の経路 (Linux でも Darwin でも
Windows でもない) に落ちる。** そこがどう振る舞うかは実際に走らせて
確かめる。

### 11.4 揃えていないところ (不足の一覧)

道具は `configure` が使う範囲に絞ってある。**検査に入っていない
ことと，不足を隠すことは別である**ので，判っている違いを並べる。
どれも `configure` の経路では踏まない。

| 道具 | 本物 | 我々 | 状態 |
|---|---|---|---|
| `grep` | 正規表現 | 固定文字列だけ | gap |
| `grep` | 開けないファイルは 2 | 1 を返す | gap |
| `grep` | ファイルが複数なら `名前:` を前置 | 前置しない | gap |
| `diff` | 差分の本文を出す | 終了コードだけ | gap |
| `head` | `-n N` も受ける | `-N` だけ | gap |
| `cp` / `mv` | 複写先にディレクトリを取れる | ファイル名だけ | gap |
| `ln` | 本物のリンク | 複写 | gap (sfs2 にリンクが無い) |
| `rm` | `-r` | 無い | gap |
| `mkdir` | `-p` 無しなら途中の段は作らない | 常に `-p` の振舞い | gap |
| `cat` の誤り文言 | `cat: X: No such file or directory` | `cat: cannot open` | gap |
| `$$` | プロセス番号 | **展開せず `$$` のまま** | gap |

`ln` を複写にしたのは sfs2 にリンクという概念が無いためである。
`configure` は `lib` / `tests` の `Makefile` を張るのに使うだけなので
複写で用が足りる。**ただし本物の意味とは違う**ので，張った先を
消しても元が残るし，元を書き換えても先に伝わらない。

`$$` は展開しない。`configure` は `TMPN="./conftest-$$"` と書くので，
我々の OS では `./conftest-$$` という名前のファイルが実際にできる。
生成物の中身には効かないので `configure` は通るが，`rm -f $TMPN*` が
葉の展開を伴うので後始末が効かず，一時ファイルが残る。**偽の
プロセス番号を返すより，展開しないほうが嘘が少ない**と判断した。

### 11.5 空装置 `/dev/null` (kernel22)

`configure` は 4 箇所で `/dev/null` を使う。3 箇所は捨てるだけだが，
1 箇所が違う。

```
$cc $OPT1 $OPT2 -o a.out -c -xc - < /dev/null > cc_msg.txt 2>&1
```

**読む**のである。「開けないから黙って捨てる」では済まない。開け
なければ `spawn` が `ENOENT` を返し，コンパイラの検出そのものが
通らない。

空のふつうのファイルを 1 つ置くのでは駄目である。読む側は確かに
0 バイトを得るが，**書くと中身が溜まる**。溜まった後に読めば以前
書いたものが返る。9.8 に並べた「黙って間違う」実装がもう 1 つ
増えるだけである。

kernel22 で項目に印 (`F_NULL = 8`) を付け，`sfsread` と `sfswrite`
の 2 箇所でだけ振り分ける。

```c
if (ld4(ent(ei) + E_FLAG) & F_NULL) return 0;   /* 読む: 常に終端 */
if (ld4(e + E_FLAG) & F_NULL) return n;         /* 書く: 全部受けて捨てる */
```

表の上ではふつうの項目なので，`walk` も `getdents64` も `chdir` も
何も知らなくてよい。`/dev` と `/dev/null` は**起動時にカーネルが
無ければ作る**。イメージは host のディレクトリを写したものなので，
「これは装置である」と書き表す場所が無いからである。

### 11.6 実測: `configure` が我々の OS で通った

第 4 部の完了条件に達した。`tests/stage016/user/cfgprobe.sh` が
kernel22 の上の `sh2` で `configure` を 2 通り走らせ，本物の POSIX
シェルの結果と突き合わせる。

```
Unsupported CPU
==== rc-nocpu 1
Binary directory    /usr/local/bin
...
Source path         /
Build OS            stone riscv32
C compiler          false (0.0)
Target OS           stone
CPU                 x86_64
Creating config.mak and config.h
==== rc-cpu 0
```

`--cpu` を渡さない 1 回目が `Unsupported CPU` で終わるのは，
**我々の機械での正しい答えである**。`uname -m` が `riscv32` を返し，
tcc の `configure` はその機種を知らない (`riscv64` は知っている)。
2 回目は機種を明示して最後まで通り，`config.h` / `config.mak` /
`config.texi` が本物と 1 バイトの違いも無く出る。

**この節は CI では走らない。** `configure` の実物が要るが，外部ソースは
repo に取り込まない決まりで (`tools/fetch.sh` の頭)，`docs/external/` は
git ignore である。Stage 14 の bzip2 / zlib と Stage 15 の tcc の検査も
同じ扱いになっている。**手元では必ず走らせること**を検査の飛ばし文言に
書いた。

飛ばした回に「通った」と読めてはいけないので，飛ばしたことは
はっきり出す。

    -- configure を我々の OS の上で走らせる --
       skip: docs/external/tcc が無い (sh tools/fetch.sh tcc で取得できる)
       ** 第 4 部の完了条件はこの節である。手元では必ず走らせること **

`--cc=false` を渡している。我々の OS にコンパイラが**コマンドとして**
無いからである。`false` は本物のシェルにも `sh2` にも組込みで，
どちらでも同じに「起動できて失敗する」。ここを `gcc` のままにすると
参照実行の側だけが本物の `gcc` を見つけ，比べる意味が無くなる。

**この突き合わせで 2 つ見つかった。** どちらも出力の比較だけでは
出てこなかったものである。

1. `uname -m -s` のように**選択肢を 2 つ並べた**とき，本物は
   `sysname machine` の順に答える。こちらは `av[1]` しか見て
   おらず `riscv32` だけを返していた。比較のために書いた身代わりの
   `uname` も同じ間違いをしていたので，**突き合わせでは露見しな
   かった**。本物の `uname` に `-m -s` を訊いて気づいた。
2. `configure` の最後の行が
   `if test "$source_path_used" = "yes" ; then ... fi` で，これが
   偽のとき `sh2` は条件の状態 1 をそのまま残していた。**生成物は
   すべて正しいのに `configure` が失敗したように見える。**
   `while` / `until` / `for` / `case` / 関数定義も同じ穴だった。
   終了コードまで突き合わせて初めて出た。

2 番目は 10.6 の「`configure` が完全に一致した」を一部訂正する。
あのとき見ていたのは**生成物と標準出力**であり，**終了コードは
見ていなかった**。一致の範囲は，測った範囲を超えない。

### 11.7 第 4 部の到達点と，次に要るもの

我々の OS の上で，シェルが 768 行の `configure` を本物と同じに
走らせる。道具は外に 1 つも要らない。残っているのは 1 つだけで，
**コンパイラをコマンドとして持つこと**である。

`configure` が外へ出た 6 回はすべてそれだった。鎖には `pp16` /
`cc15p` / `ld16` があり，どれも我々の OS の上で走るプログラムである。
足りないのは `cc -o out in.c` と書けば 3 つを順に呼ぶ**駆動役**で
ある。これは Stage 17 (tcc をこの OS の上で作る) の最初の一歩と
そのまま重なる。

## 12. Stage 16 の到達点

4 つの部をすべて終えた。**「`configure` が通る Unix」という完了条件は，
実物の `configure` を走らせて満たした。**

| 部 | 入れたもの | 世代 |
|---|---|---|
| 1 | ディレクトリを持つファイル系 (sfs2) | kernel17 |
| 2 | 作業ディレクトリと一覧・作成・移動 | kernel18 / libc16 |
| 3 | 記憶域を 14 MB から 255 MB へ | kernel19 |
| 4-1 | 削除 (`unlinkat`)。開いた fd を壊さない | kernel20 / libc17 |
| 4-2 | 標準エラーのつなぎ替えと POSIX 部分集合のシェル | kernel21 / libc18 / sh2 |
| 4-3 | 空装置 `/dev/null` と 10 個の道具 | kernel22 / sh2 |

検査は 33 件で，鎖の上で走らせるものが 9 件ある。全 Stage 通しで
392 件が通る。

### 12.1 この段で学んだこと

**「黙って間違う」実装が 6 つ出た** (9.8 に 5 件，11.6 に 1 件)。
どれも同じ形をしている —— **名前空間が平らだった頃は正しく，木が
できた瞬間に嘘になる**実装である。`open` の先頭 `/` 剥がし，
`getcwd`，`unlink`，`realpath`，kernel20 の `E_LEN` 零消し，そして
`uname` の複数選択肢。

見つけ方の内訳が重要である。

| 見つけ方 | 件数 |
|---|---|
| 既存の検査が落ちた | 2 |
| リンクエラー (多重定義) | 1 |
| コードを読んで気づいた | 2 |
| 本物と突き合わせて気づいた | 1 |

**半分は検査では見つかっていない。** 動作を壊さない誤りは動作の
検査では出ない。これは `tools/env.sh` のホスト実行パッチが緑のまま
3 回すり抜けた件 (dev-notes.md 1.2) と同じ構造である。

### 12.2 突き合わせの相手を疑うこと

11.6 の `uname` の件は，**比較のために書いた身代わりが実装と同じ
間違いをしていた**ために突き合わせをすり抜けた。身代わりは実装の
写しになりやすい。同じ人間が続けて書けばなおさらである。

対策として検査に 1 行入れた。

```sh
[ "$(uname -m -s)" = "$(uname -s) $(uname -m)" ]
```

**身代わりではなく本物に訊く。** 身代わりが要るのは値だけで，
規則は本物から取れる。

### 12.3 次に要るもの

`configure` が外へ出た 6 回はすべてコンパイラ関係だった。要るのは
`cc -o out in.c` と書けば `pp16` / `cc15p` / `ld16` を順に呼ぶ駆動役
だけである。これは Stage 17 の最初の一歩とそのまま重なる。
